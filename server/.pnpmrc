# Store directory

store-dir=.pnpm-store

# Hoist dependencies to node_modules

shamefully-hoist=true

# Auto install peers

auto-install-peers=true

# Strict peer dependencies

strict-peer-dependencies=false

# Production install

production=false
